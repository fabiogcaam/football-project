<div class="container">
    <div class="row">
        <h1 class="mt-5 mb-3">Perfil de {{user.username}}</h1>
        <hr>
        <div class="col-5">
            <figure>
                <img src="{{user.avatar}}" alt="{{user.username}}">
            </figure>
        </div>
        <div class="col-7">
            <div class="mb-3">
                <h5>Nombre de Usuario:</h5>
                <h3>{{user.username}}</h3>
            </div>
            <div class="mb-3">
                <h5>Email</h5>
                <h3>{{user.email}}</h3>
            </div>
            <div class="mb-3">
                <h5>Descripción</h5>
                <p>{{user.description}}</p>
            </div>
            <div class="row">
                <div class="col-md-6">
                    <a href="/user/{{user._id}}/edit" class="btn btn-warning btn-sm">Editar</a>
                </div>
            </div>
        </div>
    </div>
</div>