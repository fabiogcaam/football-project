<div class="container mt-5">
    <div class="row">
        <h1>Detalle de {{team.team.name}}</h1>
        <div class="col-4">
            <figure>
                <img src="{{team.team.logo}}" alt="{{team.team.name}}">
            </figure>
        </div>
        <div class="col-8">
            <h3>{{team.team.name}}</h3>
            <p>Procedente de {{this.team.country}} fue fundado en {{team.team.founded}}</p>
            <div>
                <p>Su estadio se llama {{team.venue.name}} y está situado en la ciudad de {{team.venue.city}}</p>
                <a href="/stadium/{{team.venue.id}}" class="btn btn-primary">Ver Detalle de Estadio</a>
            </div>
            <a href="/leagues" class="btn btn-dark">Volver a ligas</a>
        </div>
    </div>
</div>